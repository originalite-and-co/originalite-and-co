const slide = {
    className: 'swiper__slide'
};

const carousel = {
    effect: 'cube',
    navigation: true,
    pagination: {
        clickable: true
    },
    className: "popularProdCard"
};

export default { slide, carousel };