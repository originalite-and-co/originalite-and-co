export function generateStyles() {
  return {
    nav: {
      paddingTop: '14px',
      paddingBottom: '14px',
      position: 'relative',
      backgroundColor: '#000000',
      zIndex: 'unset'
    },
    inner: {
      display: 'flex',
      justifyContent: 'space-between'
    }
  };
}
